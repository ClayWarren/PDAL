[package]
name = "pdal-plugins"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Plugin metadata and discovery helpers for the PDAL Rust port."

[dependencies]
pdal-core = { path = "../pdal-core" }
