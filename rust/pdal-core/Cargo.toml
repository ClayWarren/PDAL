[package]
name = "pdal-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimal PDAL core (point model, stages, options) -- Rust port spike."

[dependencies]
getrandom = "0.2"
pdal-native = { path = "../pdal-native" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
