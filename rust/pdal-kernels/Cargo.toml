[package]
name = "pdal-kernels"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Placeholder crate for PDAL Rust CLI kernels."

[dependencies]
pdal-core = { path = "../pdal-core" }
