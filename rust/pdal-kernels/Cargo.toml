[package]
name = "pdal-kernels"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "CLI kernels for the PDAL Rust port."

[dependencies]
chrono = "0.4"
glob = "0.3"
pdal-core = { path = "../pdal-core" }
serde_json = "1"
